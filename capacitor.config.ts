import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourname.securtradeai',
  appName: 'SecureTrade AI',
  webDir: 'public'
};

export default config;
